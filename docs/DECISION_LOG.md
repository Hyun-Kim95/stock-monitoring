# 의사결정 로그 (외부 API · 인프라 · 운영)

**용도:** 합의가 없으면 체크만 하고 넘어가기 쉬운 항목을 **문서에 먼저 고정**하고, 바뀌면 아래에 개정 행을 추가합니다.  
**상태:** `locked` = 당분간 바꾸지 않음 · `provisional` = 키/약관 확인 후 바뀔 수 있음.

---

## D-001 — 실시간 시세: 1차 연동 대상

| 항목 | 내용 |
|------|------|
| 상태 | **provisional** (모의·실전 앱키 발급 후 확정) |
| 1순위 | **한국투자증권 KIS Developers** (국내 주식 REST/WebSocket, OAuth2 `appkey`/`appsecret` 흐름) |
| 그때까지 | 기본 시드는 `mock`; **구현됨:** `market_data.provider=kis` + `.env`의 `KIS_APP_KEY`/`KIS_APP_SECRET`이 있으면 REST 현재가 폴링, 없거나 설정이 mock이면 목 시세. 현재가 응답의 **외국인 순매수 수량·소진율**(`frgn_ntby_qty`, `hts_frgn_ehrt`)을 `QuoteSnapshot`에 매핑해 대시보드 표시 |
| 대안 | 타 증권사 Open API, 유료 시세 벤더 (스키마는 `QuoteSnapshot`으로 정규화) |
| 장애 시 | 목 프로바이더로 폴백(설정 키로 전환) 검토 |

### 후보 비교표 (초안 — 한도·WS는 **키 발급 후** 셀 채움)

| 후보 | 실시간 체결/호가 | WebSocket | 인증 | 비고 |
|------|------------------|-----------|------|------|
| KIS Developers | 조사·상품별 상이 → **발급 후 기재** | 지원 범위 문서 확인 | OAuth2 + appkey | 1순위 구현 대상 |
| 기타 증권사 Open API | 벤더별 | 벤더별 | 벤더별 | D-001 개정 시 행 추가 |
| 목(mock) | 해당 없음 | 해당 없음 | 해당 없음 | 현재 기본 |

---

## D-002 — 뉴스: 1차 연동 대상

| 항목 | 내용 |
|------|------|
| 상태 | **provisional** (Client ID 발급·약관 검토 후 확정) |
| 1순위 | **네이버 오픈API — 뉴스 검색** (`openapi.naver.com`, 검색 API의 `news` 타입) |
| 검색어 구성 | `stock.name` + `searchAlias`(쉼표 분리) + `NewsSourceRule` (이미 코드 반영 방향) |
| 그때까지 | **구현됨:** 키 있으면 네이버 검색; **0건이면 빈 목록**(목으로 채우지 않음); **실패 시 HTTP 502** `NEWS_UPSTREAM_FAILED` + 원인 메시지. 키 없을 때만 목 뉴스. 규칙 필터·URL 중복 제거·TTL 동일 |
| 약관 | 상업 이용·일일 호출 한도·표시 의무는 **법무/운영 검토 후** 체크리스트 완료 |

### 중복·노이즈 (운영 규칙)

- **중복 제거:** URL 우선 (동일 URL 1건만) — 이미 구현.
- **노이즈 샘플 테스트:** `docs/NEWS_NOISE_TEST_PLAN.md` 절차로 진행 (API 연동 후 실측).

---

## D-003 — 관리자·보안 (MVP 운영 최소)

| 항목 | 내용 |
|------|------|
| 상태 | **locked** (MVP) |
| 레이어 1 | `ADMIN_API_TOKEN` — 프로덕션에서는 **반드시 설정**, `Authorization: Bearer` |
| 레이어 2 (권장) | 리버스 프록시에서 관리자 UI 또는 쓰기 API 경로 **IP 허용 목록** 또는 **추가 Basic Auth** |
| 시크릿 저장 | 서버 env / 비밀 저장소; DB `system_settings` 민감값은 응답 **마스킹** (현 구현 유지) |
| 레이트 리밋 | 비-GET IP당 분당 120회 (`write-rate-limit.ts`) |

---

## D-004 — 배포·인프라 타깃 (MVP)

| 항목 | 내용 |
|------|------|
| 상태 | **locked** (MVP) |
| 형태 | **단일 VM** 에 `web` + `api` 프로세스, PostgreSQL은 동일 호스트 또는 관리형 DB |
| 로컬 | PostgreSQL 로컬 설치(또는 원격); `DATABASE_URL`로 연결 |
| TLS | **Caddy 또는 Nginx** 종단, WS 업그레이드 허용 |
| API | 호스트 프로세스(`tsx`/`node`) + 프로세스 매니저 권장 |

---

## D-005 — 관심종목 수·실시간 주기 (SLA)

| 항목 | 내용 |
|------|------|
| 상태 | **locked** (숫자는 `system_settings`로 조정 가능) |
| 보장 목표 | 활성 종목 **50개까지** UX·성능 기준으로 설계 |
| 하드 상한 | 활성 종목 **최대 100개** — 초과 시 등록/재활성화 거절 (`stocks.max_active`) |
| 시세 틱(목) | 1초 |
| 브로드캐스트 | `realtime.broadcast_throttle_ms` (기본 250ms) |

---

## D-006 — 외부 장애 시 사용자 메시지 (정책만 선행)

| 항목 | 내용 |
|------|------|
| 상태 | **provisional** (어댑터 연동 시 구현) |
| 원칙 | 외부 시세 끊김 시 WS `status.marketConnected = false` + 짧은 한글 메시지 |
| UI | 대시보드 상단 배너 (추가 작업) |

---

## 개정 기록

| 날짜 | ID | 변경 |
|------|-----|------|
| 2026-03-21 | D-001~D-006 | 최초 합의안 작성 |
